# pin-service — evidence

_Inventory generated 2026-06-29T10:36:38Z. The serving pattern is a tested reference implementation; tests run locally with the repo deps and were not re-run in this environment._

Reproduced engineering (the serving path runs); the GBDT scorer uses a synthetic label and the map/supply signals are fixtures, so the model and data are illustrative.

## Test suite (unit per stage + Locust load test)
```
```
